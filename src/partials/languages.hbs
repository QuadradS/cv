<section class="languages section">
  <h2 class="section__title">{{labels.languages}}</h2>
  <ul class="languages__list">
    {{#each languages}}
      <li class="languages__item"><strong>{{name}}</strong> — {{level}}</li>
    {{/each}}
  </ul>
</section>
